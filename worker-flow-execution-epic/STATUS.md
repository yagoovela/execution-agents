# Status — the single source

**This table is the only place a task's state is written down.** The task pages and the timeline
read it at build time; nothing else records progress. Edit here, re-run the build, and every page
agrees. If you find status stated anywhere else, that copy is a bug — delete it and point at this
file. See `MAINTENANCE.md` for when to touch it.

`state` is one of:

| state | meaning |
|---|---|
| `planned` | Written down, not started. The default. |
| `blocked` | Cannot start — the `note` says what is in the way (a decision, another task, a measurement). |
| `doing` | Someone is working on it right now. |
| `review` | Code is up; the negative controls have been run and reported. |
| `shipped` | In production, with the definition of done satisfied. |
| `dropped` | Deliberately not doing it. The `note` says why, and that is a decision worth keeping. |

`ref` is the PR, branch or ClickUp id. Leave it `—` until there is one.

`note` is bilingual: `English // Português`, split on ` // ` at build time. A note without the
separator fails the build and names the task, because the pages are bilingual and the note is
rendered into them verbatim — an untranslated note would show English in both languages. Leave the
cell empty when there is nothing to say.

| task | state | ref | note |
|---|---|---|---|
| S7 | shipped | 868m48692 | Parts 1 and 3 (webhook route retired to a 410 and the `public` predicate) and part 2 (rate limiting on the five original run-creating routes) are in production. The follow-up (868m48692) adds the remaining five routes — beta-v2, execute-single-node, chatbot-api-socket, formapi-v1 and run-public-flow — and an anonymous public-flow caller now keys by flow instead of filling the owner organisation's bucket; it passed QA and is merged to dev, with PR #1975 open against production. Marked `shipped` ahead of the merge because the production deploy is scheduled for 2026-09-21; the Done when clause "every run-creating route is limited by the paying entity" becomes true in production when #1975 lands with it. Everything ships in report mode, so nothing is refused yet. // As partes 1 e 3 (rota de webhook aposentada para 410 e o predicado `public`) e a parte 2 (rate limit nas cinco rotas originais que criam run) estão em produção. O follow-up (868m48692) acrescenta as cinco rotas restantes — beta-v2, execute-single-node, chatbot-api-socket, formapi-v1 e run-public-flow — e um chamador anônimo de fluxo público agora é contado pelo fluxo em vez de encher o balde da organização dona; passou no QA e está mergeado em dev, com o PR #1975 aberto para produção. Marcada como `shipped` antes do merge porque o deploy para produção está agendado para 2026-09-21; a cláusula de conclusão "toda rota que cria run é limitada pela entidade pagante" passa a valer em produção quando o #1975 entrar junto. Tudo sobe em modo report, então nada é recusado ainda. |
| S1 | shipped | 868m0vfyz | In production since 2026-09-16: the pre-flight gate refuses a cascade deeper than the root flow's maxCascadeSize and a cycle in the call graph, as HTTP 422 before enqueueing. The front carries no entry for cascade_size or cascade_cycle, which is latent rather than broken — the gate returns before the run loop, so those ceilings never travel the socket path that drops an unknown ceiling silently. // Em produção desde 16/09/2026: o gate pré-execução recusa uma cascata mais profunda que o maxCascadeSize do fluxo raiz e um ciclo no grafo de chamadas, como HTTP 422 antes de enfileirar. O front não tem entrada para cascade_size nem cascade_cycle, o que é latente e não quebrado — o gate retorna antes do loop do run, então esses tetos nunca passam pelo caminho de socket que descarta um teto desconhecido em silêncio. |
| S4 | shipped | feat/868m0vfby | D15 measured on dev+staging (loopCount p50 3 / max 20; processorArray p50 7 / p95 81 / max 963; node executions per run max 42), and the prod measurement (2026-09-03: loopCount max 40; processorArray max 8555; per-run max 438, an undercount) closed the ceilings at 150 / 12000 / 50000 / 200000 behind the execution_budget flag — a 1.1-1.5x margin over the real bound, X taken over the UI's own max of 100; Y stays provisional until s4-prod-run-executions.sql runs; the env script confirmed dev/staging/prod declare none of the five variables and all run the legacy engine, so nothing needs an env deploy and only the DB flag is left; per-company/per-agent ceilings + screen are follow-up 868m10e1z. Y and the credit balance are separate ceilings stopping the same run for different reasons, and D20 makes both behave the same way. In production since 2026-09-10. // D15 medida em dev+staging (loopCount p50 3 / máx 20; processorArray p50 7 / p95 81 / máx 963; máx 42 execuções de nó por run), e a medição em prod (2026-09-03: loopCount máx 40; processorArray máx 8555; máx 438 por run, subcontado) fechou os tetos em 150 / 12000 / 50000 / 200000 atrás do flag execution_budget — margem de 1,1-1,5x sobre o limite real, o X tomado sobre o próprio máx 100 da UI; o Y segue provisório até rodar s4-prod-run-executions.sql; o script de env confirmou que dev/staging/prod não declaram nenhuma das cinco variáveis e rodam o engine legacy, então não há env para deployar e resta só o flag no banco; tetos por empresa/agente + tela ficaram no follow-up 868m10e1z. O Y e o saldo de crédito são tetos distintos que param o mesmo run por motivos diferentes, e a D20 faz os dois se comportarem igual. Em produção desde 10/09/2026. |
| S5 | shipped | 868m0vfhf | In production since 2026-09-16. // Em produção desde 16/09/2026. |
| C3 | shipped | 868m0vfrz | In production since 2026-09-16. // Em produção desde 16/09/2026. |
| S2 | shipped | feat/868m0vjwk | Worker pool now sets an explicit `max` of 12, derived and not overridable: `MAX_CONCURRENT_ACTIVITY_TASK_EXECUTIONS` + 2 for the boot health check and anything outside an activity, with the concurrency constant living in one place (`worker/src/modules/temporal/temporal.constants.ts`) so `worker.service.ts` and `database.service.ts` cannot drift. There is deliberately no `WORKER_DB_POOL_MAX` override: the spec's own reasoning says a pool above activity concurrency cannot help, so the only knob it would give an operator is the one that breaks the invariant. Honest framing — node-postgres already defaulted to 10 per pool, so on the `max` alone this makes the ceiling explicit (and 2 higher) rather than lower; the real reduction came from a second defect found while validating this locally (below). Also set: `idleTimeoutMillis` (default 30000 via `WORKER_DB_POOL_IDLE_TIMEOUT_MS`; 10000 would have been the driver default and would drain the pool between activity bursts, paying a fresh TCP+auth handshake per connection on the next burst) and `connectionTimeoutMillis` (default 5000 via `WORKER_DB_POOL_CONNECTION_TIMEOUT_MS`, comfortably under the shortest activity `startToCloseTimeout` of 10 seconds). Negative control run before trusting the fix: with the 12 pool slots held and `connectionTimeoutMillis` removed, the next `connect()` hung and had to be killed at 60s; restored, it rejects naming "timeout exceeded when trying to connect". The test follows the repo's real-Postgres convention (`RUN_DB_POOL_INTEGRATION` gate, `describe.skip` otherwise), so `pnpm test` does not require a database. The `max = maxConcurrentActivityTaskExecutions + 2` assumption is confirmed, not revised. Second defect, found by measuring and not by reading: `pull-data`, `push-data` and `web-crawling` each re-declared `DatabaseService` in their own `providers` array, so Nest built a module-scoped instance per module instead of injecting the `@Global()` singleton — the process ran four pools, 48 connections, and the `max` never bounded a replica at all. The tell was `pool.totalCount` reporting 1 while `pg_stat_activity` showed 4 backends for the same PID. `database-provider.spec.ts` asserts the registration statically and was seen red with the declaration put back. Local validation against the compiled worker (real `AppModule`, local Postgres, `PGAPPNAME` per process): three replicas each capped at exactly 12 connections, 36 total, linear in replica count, 18 of 30 oversubscribed operations refused at ~5002ms instead of queueing, and the pool drained to 0 at rest. Acquisition latency measured against the 5000ms limit before trusting a rule that refuses: p50 0.026ms / max 0.24ms warm, p50 5.5ms / max 13.8ms cold — loopback without TLS, so RDS will be higher, but the margin is three orders of magnitude. Worst realistic burst (10 concurrent `applyCounterDelta` on the same counter, serialized on the advisory lock) peaked at 10 connections with 0 waiting, so 12 covers it with 2 spare. Boot with the database unreachable now fails at 5029ms instead of hanging, so the orchestrator restarts rather than the process sitting dead. Long-activity interaction, checked after the fact: activity duration does not hold a connection — 27 call sites use `pool.query` (acquire, query, release) and only two check out a client, both self-contained. A 20-minute text generation holds zero connections while it runs, so long activities lower average connection pressure rather than raising it. But the cost of a false refusal is higher than the probability suggests: `workflows/configs.ts` sets `startToCloseTimeout` up to 30 minutes with `retry: { maximumAttempts: 1 }`, so a pool timeout on a query at minute 19 destroys 19 minutes of work with no second attempt. That is still better than the pre-fix behaviour (hang until the 30-minute timeout, then fail anyway), but it means the 5000ms limit must never be tightened without measuring, and the one place that can plausibly saturate the pool — the OAuth refresh holding a connection for a 15–30s provider round trip — is recorded as C3 item 7 rather than fixed here. Real-flow measurement, closing the spec's remaining verification bullet: the full local stack (back + worker + Temporal + Postgres, worker built from this branch) running a real flow through the real API. Twenty concurrent runs on distinct sessions and sixty concurrent runs on the same session (maximum advisory-lock contention) both peaked the worker at **5 of 12** connections, 1 at rest, with the API peaking at 19 and the whole database at 21. The worker never reached its ten activity slots in this stack — in-flight activities stayed at about five, and the cause is upstream of the pool and was not identified, so this run confirms 12 is not binding under real traffic but does **not** exercise the ten-slot worst case; that case remains covered by the synthetic measurement (peak 10, 0 waiting). Capacity arithmetic: prod `max_connections` is 1716 (fingerprinted via `psql`, `.workflow/kb/db-migrations/00-connection-guide.md`); reserving ~50 connections as a stated, unverified-from-this-repo assumption for migrations (the back's own `migrationsRun` reuses its own pool at boot, so this is mostly ad-hoc operator sessions) leaves 1666 for `api_pool` + `worker_pool`. Confirmed against the real AWS account (read-only, `mcp-access`): `enhancedai-prod` runs **one** API task and **one** worker task (desired = running = 1 for both), and `TYPEORM_POOL_MAX` appears in neither the API task definition's `environment` nor its 155 SSM-backed `secrets`, so the API pool is the code default of **30** (`back/src/config/config.service.ts:7`). No `WORKER_DB_POOL_*` exists in the worker task definition either, which is the intended outcome, not an oversight: the values are optional and derived, so prod needs no change. `mcp-prod` holds no Postgres connection at all (it reaches the API over `BACK_BASE_URL`), so `api + worker` is the complete formula today. So 1666 − 30 = 1636, so `floor(1636 / 12)` = **136 worker replicas** the current database supports — a figure that only holds because of the provider fix; at the 48 connections a replica really opened before it, the same database supported 34; if API and worker replicas scale together N:N, N × 42 ≤ 1666 → N ≤ 39. Both are a mathematical ceiling, not a target — Temporal's own workflow/activity concurrency binds long before either number. Two things the AWS read added beyond closing the gap. First, the configured ceiling in prod today is 30 + 12 = **42 of 1716**, about 2.4% — and before the provider fix it would have been 30 + 48 = 78, so the fix cut the worker's share by four even at one replica. Second, `max_connections` is not a fixed number: `enhancedai-db-prod` is a `db.t3.xlarge` on `default.postgres14`, where the parameter is `LEAST({DBInstanceClassMemory/9531392},5000)`. The 1716 fingerprint is consistent with that class, but the ceiling is derived from instance memory, so downsizing the instance lowers it proportionally and every replica count above has to be recomputed. The actual connection counts in prod were **not** measured — these are configured maxima, and this epic does not query the production database. Connection-proxy decision: recommend PgBouncer in transaction mode, recorded but not deployed in this PR — no proxy config exists in this codebase's infra to change. The constraint holds either way: transaction mode forbids session-level state, and both existing advisory locks (`oauth-token.repo.ts`, the counter lock in `session-store.ts`) use `pg_advisory_xact_lock`, transaction-scoped, so they survive; there are no session-scoped advisory locks today. Left open: confirm the real prod `TYPEORM_POOL_MAX` and today's actual back/worker replica counts against AWS, not the repo's assumptions. `env-vars-sync` ran: `WORKER_DB_POOL_IDLE_TIMEOUT_MS` and `WORKER_DB_POOL_CONNECTION_TIMEOUT_MS` added to `worker/.env.example` and the audit/Mintlify docs. QA passed; the production deploy is scheduled for 2026-09-21. // O pool do worker agora define um `max` explícito de 12, derivado e não sobrescrevível: `MAX_CONCURRENT_ACTIVITY_TASK_EXECUTIONS` + 2 para o health check de boot e o que abre conexão fora de uma activity, com a constante de concorrência morando num único lugar (`worker/src/modules/temporal/temporal.constants.ts`) para que `worker.service.ts` e `database.service.ts` não possam divergir. Não existe override `WORKER_DB_POOL_MAX` de propósito: o próprio raciocínio da spec diz que um pool acima da concorrência de activities não ajuda, então o único botão que isso daria ao operador é o que quebra o invariante. Enquadramento honesto — o node-postgres já usava 10 por pool como default, então só pelo `max` isto torna o teto explícito (e 2 maior), não o reduz; a redução real veio de um segundo defeito encontrado ao validar isto localmente (abaixo). Também definidos: `idleTimeoutMillis` (default 30000 via `WORKER_DB_POOL_IDLE_TIMEOUT_MS`; 10000 seria o default do driver e drenaria o pool entre rajadas de activities, pagando TCP+auth novos por conexão na rajada seguinte) e `connectionTimeoutMillis` (default 5000 via `WORKER_DB_POOL_CONNECTION_TIMEOUT_MS`, confortavelmente abaixo do menor `startToCloseTimeout` de activity, 10 segundos). Controle negativo rodado antes de confiar na correção: com as 12 vagas do pool ocupadas e o `connectionTimeoutMillis` removido, o `connect()` seguinte travou e precisou ser morto aos 60s; restaurado, ele rejeita citando "timeout exceeded when trying to connect". O teste segue a convenção do repo para Postgres real (gate `RUN_DB_POOL_INTEGRATION`, `describe.skip` caso contrário), então o `pnpm test` não exige banco. A suposição `max = maxConcurrentActivityTaskExecutions + 2` está confirmada, não revisada. Segundo defeito, encontrado medindo e não lendo: `pull-data`, `push-data` e `web-crawling` redeclaravam `DatabaseService` no próprio array de `providers`, então o Nest construía uma instância por módulo em vez de injetar o singleton `@Global()` — o processo rodava quatro pools, 48 conexões, e o `max` nunca limitou uma réplica. O sinal foi `pool.totalCount` reportando 1 enquanto o `pg_stat_activity` mostrava 4 backends para o mesmo PID. O `database-provider.spec.ts` verifica o registro estaticamente e foi visto vermelho com a declaração de volta. Validação local contra o worker compilado (`AppModule` real, Postgres local, `PGAPPNAME` por processo): três réplicas limitadas a exatamente 12 conexões cada, 36 no total, linear na contagem de réplicas, 18 de 30 operações excedentes recusadas em ~5002ms em vez de enfileirar, e o pool drenou para 0 em repouso. Latência de aquisição medida contra o limite de 5000ms antes de confiar numa regra que recusa: p50 0,026ms / máx 0,24ms a quente, p50 5,5ms / máx 13,8ms a frio — loopback sem TLS, então o RDS será maior, mas a margem é de três ordens de grandeza. Pior rajada realista (10 `applyCounterDelta` concorrentes no mesmo contador, serializados no advisory lock) teve pico de 10 conexões com 0 em espera, então 12 cobre com 2 de folga. Boot com o banco inalcançável agora falha em 5029ms em vez de travar, então o orquestrador reinicia em vez de o processo ficar morto. Interação com activities longas, verificada depois: a duração da activity não segura conexão — 27 pontos usam `pool.query` (pega, consulta, devolve) e só dois fazem checkout de um cliente, ambos autocontidos. Uma geração de texto de 20 minutos segura zero conexões enquanto roda, então activities longas reduzem a pressão média de conexões em vez de aumentá-la. Mas o custo de uma recusa falsa é maior do que a probabilidade sugere: o `workflows/configs.ts` define `startToCloseTimeout` de até 30 minutos com `retry: { maximumAttempts: 1 }`, então um timeout de pool numa query no minuto 19 destrói 19 minutos de trabalho sem segunda tentativa. Ainda é melhor que o comportamento anterior (travar até o timeout de 30 minutos e falhar do mesmo jeito), mas significa que o limite de 5000ms nunca deve ser apertado sem medir, e o único lugar que pode plausivelmente saturar o pool — o refresh de OAuth segurando uma conexão por uma ida e volta de 15–30s ao provedor — ficou registrado como item 7 da C3 em vez de corrigido aqui. Medição com flow real, fechando o item de verificação que faltava na spec: a stack local completa (back + worker + Temporal + Postgres, worker construído a partir deste branch) rodando um flow real pela API real. Vinte runs concorrentes em sessões distintas e sessenta runs concorrentes na mesma sessão (contenção máxima no advisory lock) tiveram ambos pico de **5 de 12** conexões no worker, 1 em repouso, com a API em 19 e o banco inteiro em 21. O worker nunca chegou às suas dez vagas de activity nesta stack — as activities em voo ficaram em torno de cinco, e a causa está acima do pool e não foi identificada, então esta execução confirma que 12 não é o limitante sob tráfego real mas **não** exercita o pior caso de dez vagas; esse caso segue coberto pela medição sintética (pico 10, 0 em espera). Aritmética de capacidade: `max_connections` de prod é 1716 (fingerprint via `psql`, `.workflow/kb/db-migrations/00-connection-guide.md`); reservando ~50 conexões como suposição declarada e não verificável a partir deste repo para migrations (o `migrationsRun` do back reusa o próprio pool no boot, então isso é majoritariamente sessões ad-hoc de operador) sobram 1666 para `api_pool` + `worker_pool`. Confirmado contra a conta AWS real (somente leitura, `mcp-access`): o `enhancedai-prod` roda **uma** task de API e **uma** task de worker (desired = running = 1 nas duas), e o `TYPEORM_POOL_MAX` não aparece nem no `environment` da task definition da API nem nos seus 155 `secrets` vindos do SSM, então o pool da API é o default do código, **30** (`back/src/config/config.service.ts:7`). Também não existe nenhum `WORKER_DB_POOL_*` na task definition do worker, e isso é o resultado pretendido, não um esquecimento: os valores são opcionais e derivados, então prod não precisa de mudança. O `mcp-prod` não segura conexão de Postgres alguma (fala com a API por `BACK_BASE_URL`), então `api + worker` é a fórmula completa hoje. Logo 1666 − 30 = 1636, então `floor(1636 / 12)` = **136 réplicas de worker** que o banco atual suporta — número que só vale por causa da correção dos providers; com as 48 conexões que uma réplica realmente abria antes dela, o mesmo banco suportava 34; se API e worker escalarem juntas N:N, N × 42 ≤ 1666 → N ≤ 39. Ambos são um teto matemático, não uma meta — a concorrência de workflow/activity do próprio Temporal esbarra bem antes de qualquer um dos dois números. Duas coisas que a leitura da AWS trouxe além de fechar a lacuna. Primeiro, o teto configurado em prod hoje é 30 + 12 = **42 de 1716**, cerca de 2,4% — e antes da correção dos providers teria sido 30 + 48 = 78, ou seja, a correção cortou por quatro a fatia do worker mesmo com uma réplica. Segundo, o `max_connections` não é um número fixo: o `enhancedai-db-prod` é um `db.t3.xlarge` no `default.postgres14`, onde o parâmetro é `LEAST({DBInstanceClassMemory/9531392},5000)`. O fingerprint de 1716 é consistente com essa classe, mas o teto deriva da memória da instância, então reduzir a classe o baixa proporcionalmente e toda contagem de réplicas acima precisa ser recalculada. As conexões reais em prod **não** foram medidas — estes são máximos configurados, e este épico não consulta o banco de produção. Decisão de proxy de conexão: recomenda-se PgBouncer em modo transação, registrada mas não implantada nesta PR — não existe config de proxy neste repositório para mudar. A restrição vale de qualquer forma: modo transação proíbe estado de sessão, e os dois locks consultivos existentes (`oauth-token.repo.ts`, o lock do contador em `session-store.ts`) usam `pg_advisory_xact_lock`, com escopo de transação, então sobrevivem; não há locks consultivos de sessão hoje. Nada em aberto nos números de capacidade: o tamanho do pool de prod e as contagens de réplicas agora são lidos da AWS em vez de supostos. O `env-vars-sync` rodou: `WORKER_DB_POOL_IDLE_TIMEOUT_MS` e `WORKER_DB_POOL_CONNECTION_TIMEOUT_MS` adicionadas ao `worker/.env.example` e à documentação de auditoria/Mintlify. QA aprovado; o deploy para produção está agendado para 2026-09-21. |
| S3 | shipped | feat/868m0vk05 | Charge-time spend ceiling accounted against the chain root (default 100000 credits, counted in credits not cost because updateTokens forces cost to 0 for trial and prepaid), plus a per-tenant concurrency cap that defers rather than refuses (default 25, measured: the busiest org held 21 at once). Both behind flags seeded disabled: spend_ceiling, tenant_concurrency_cap. Running the fairness procedure against a real Redis found the cap's admission was not atomic — 30 of 30 simultaneous bursts admitted nobody where 3 should have — now one Lua script, 30 of 30 correct. Left for QA: Test Case 1 end to end, which needs provider credentials and spends real money. D19 makes fluxCred exhaustion stop the run at the node boundary; D20's pre-flight credit check is satisfied by S1's gate, so S3 adds no second refusal there. Measured on dev after the first deploy, and fixed: a node dispatched to the worker mints its own execId, and nothing mapped that id to the run's chain root, so the charge path resolved it to itself and every node execution billed as a chain of one — the ledger never held a run total, and when a single charge did breach on its own the cancel was addressed to an id that names no run. Four of five runs of one dev flow exceeded a 1-credit budget by up to 4x while logging success. ChainRootRegistry.adopt now points a dispatched node at the root its run already registered, which also carries a sub-flow's nodes onto the parent's chain. A second defect surfaced the same way: the ceiling was resolved from the flow each charge belonged to, not from the run's root flow, so a root's budget governed only the nodes living in the root. Measured on dev with a root at 0.2 credits calling a sub-flow at 0.1 — the root charges nothing, so its 0.2 was never consulted and the chain ran under the sub-flow's 0.1; the same shape with an unset sub-flow budget releases the chain to the account limit. The root now records its budget once when it opens the chain, only it may write and only once, a root without a budget is recorded explicitly so a nested flow cannot govern by omission, and the budget is fixed for the life of the run. Validated on dev with four real runs: each stops naming 0.2 with a single execId per chain, where before the fix the same flow ran both sub-flow calls and named 0.1. Known limit, not a defect: enforcement happens after a charge lands, so the charge that crosses the ceiling is always paid in full — the overshoot is one charge, and one expensive node can make it large. QA passed on 2026-09-17; PR #1976 is open against production and is scheduled to deploy on 2026-09-21, which is why the row already reads `shipped` rather than waiting for the merge. One finding from the dev analysis was deliberately left out of this task: a run whose flow calls other agents logs `totalTokens = 0` however much the chain charged, because the called agent runs in its own accumulator and the caller drops what it returns. It is a fix derived from S3, not a prerequisite for it — the ceiling reads `token_transaction` and the Redis ledger, never that number — so it went to 868m6r9py in `up next`, and E1 carries a pointer because whoever retires `onUpdateTokens` passes through the same code. Deployed to production on 2026-09-21. // Teto de gasto no momento da cobrança, contabilizado na raiz da cadeia (padrão 100000 créditos, em créditos e não em custo porque o updateTokens zera o custo para trial e pré-pago), mais um cap de concorrência por tenant que adia em vez de recusar (padrão 25, medido: a org mais movimentada segurou 21 ao mesmo tempo). Ambos atrás de flags semeadas desligadas: spend_ceiling, tenant_concurrency_cap. Rodar o procedimento de fairness contra um Redis real revelou que a admissão do cap não era atômica — 30 de 30 rajadas simultâneas não admitiram ninguém onde 3 deveriam entrar — agora é um único script Lua, 30 de 30 corretas. Para o QA: o Test Case 1 ponta a ponta, que exige credenciais de provedor e gasta dinheiro real. A D19 faz o fim do fluxCred parar o run no limite do nó; a checagem de crédito pré-execução da D20 é atendida pelo gate da S1, então a S3 não acrescenta uma segunda recusa ali. Medido em dev depois do primeiro deploy, e corrigido: um nó despachado para o worker gera o próprio execId, e nada mapeava esse id para a raiz da cadeia do run, então o caminho de cobrança o resolvia para si mesmo e cada execução de nó era cobrada como uma cadeia de um — o ledger nunca guardava o total do run, e quando uma cobrança sozinha estourava o teto o cancelamento era endereçado a um id que não nomeia run nenhum. Quatro de cinco runs de um flow em dev passaram de um orçamento de 1 crédito em até 4x registrando success. O ChainRootRegistry.adopt agora aponta o nó despachado para a raiz que o run já registrou, o que também leva os nós de um sub-flow para a cadeia do pai. Um segundo defeito apareceu pelo mesmo caminho: o teto era resolvido a partir do fluxo de cada cobrança e não do fluxo raiz do run, então o orçamento da raiz governava apenas os nós que viviam nela. Medido em dev com uma raiz de 0,2 créditos chamando um sub-fluxo de 0,1 — a raiz não cobra nada, então os 0,2 dela nunca eram consultados e a cadeia rodava sob os 0,1 do sub-fluxo; o mesmo formato com o sub-fluxo sem orçamento solta a cadeia para o limite da conta. Agora a raiz grava o próprio orçamento uma vez ao abrir a cadeia, só ela escreve e só uma vez, uma raiz sem orçamento é gravada explicitamente para que um fluxo aninhado não governe por omissão, e o orçamento fica fixo pela vida do run. Validado em dev com quatro execuções reais: cada uma para citando 0,2 com um único execId por cadeia, onde antes da correção o mesmo fluxo executava as duas chamadas de sub-fluxo e citava 0,1. Limitação conhecida, não defeito: a checagem acontece depois que a cobrança entra, então a cobrança que cruza o teto é sempre paga por inteiro — o estouro é de uma cobrança, e um nó caro pode torná-lo grande. QA aprovado em 2026-09-17; o PR #1976 está aberto para production e o deploy está agendado para 2026-09-21, por isso a linha já aparece como `shipped` em vez de esperar o merge. Um achado da análise em dev ficou deliberadamente fora desta task: um run cujo fluxo chama outros agentes registra `totalTokens = 0` por mais que a cadeia tenha cobrado, porque o agente chamado roda no próprio acumulador e quem chamou descarta o que ele devolve. É uma correção derivada da S3, não um pré-requisito dela — o teto lê `token_transaction` e o ledger no Redis, nunca esse número — então foi para a 868m6r9py em `up next`, e a E1 leva um ponteiro porque quem aposentar o `onUpdateTokens` passa pelo mesmo código. Deployado em produção em 21/09/2026. |
| S8 | review | feat/868m0vkve | Part 1: every framework cron now runs inside `CronLockService.runExclusively`, a `pg_try_advisory_xact_lock` held for the life of the job, so N replicas do the work once instead of N times — `DISABLE_CRONS` was absorbed into the same guard. Part 2 took D26 (Bull repeatable jobs over the queue already in production) rather than a Temporal bridge workflow that B4 would delete; user schedules register one repeatable per schedule, the consumer re-reads the schedule, unregisters itself when the schedule or its flow is gone, and enqueues the run without waiting for it. Behind `repeatable_schedules`, seeded disabled. Part 4: inbound email is claimed in Redis with `SET <key> '1' EX 86400 NX` keyed on `Message-ID` (sha256 of the raw message when absent), and the same key rides along as the `api-v2` `jobId`. Part 3 went to E5 and the durable half of Part 2 to E4. Measured on two real replicas sharing one Postgres and one Redis, each control run a second time against a deliberately broken copy so the failure was observed: cron lock 1 executed / 1 skipped (broken: 2 interleaved); schedules 1 repeatable entry and 11 fires in 21s alternating replicas (broken: 2 entries, 20 fires); trigger completes in 14–68ms with one run per tick (broken: 3023ms); two identical `Message-ID`s produce one run. Recorded in `S8-REPLICA-HARNESS.md` with the probes in `scripts/s8-controls/`. One honest exception: `listPop3Emails` registers POP3 event handlers and returns, so its transaction commits while the conversation is still in flight and 4 of 22 ticks ran on both replicas at once. The lock is not at fault — it is exclusive whenever the job awaits its own work — and what protects inbound email is the Redis claim, not the lock; covering the poll would mean holding a Postgres transaction open across the whole conversation every tick. Control 5 also demonstrated the E5 risk concretely: under `volatile-lru` 0 of 100 dedup keys survived a fill; under `noeviction` all survived and the filling write took the OOM instead. Validated in dev on 2026-09-21 with the flag on: five consecutive two-minute ticks produced exactly one run each, and soft-deleting the schedule stopped the runs at the next fire with no restart, the consumer removing its own repeatable. Two things changed how the flag rolls out: the reconcile lives in `onModuleInit`, so turning it on in a running process registers nothing until the next boot; and it used to leave `delete()` on the new path while the in-process `CronJob` from boot kept running and charging, so teardown no longer sits behind the flag. Passed QA on 2026-09-21; waiting in the deploy queue. // Parte 1: todo cron de framework agora roda dentro do `CronLockService.runExclusively`, um `pg_try_advisory_xact_lock` mantido pela vida do job, então N réplicas fazem o trabalho uma vez e não N vezes — o `DISABLE_CRONS` foi absorvido pela mesma guarda. A parte 2 seguiu a D26 (repeatable jobs do Bull sobre a fila que já está em produção) em vez de um workflow-ponte no Temporal que a B4 apagaria; os schedules do usuário registram um repeatable por schedule, o consumer relê o schedule, se desregistra quando o schedule ou o flow some, e enfileira a execução sem esperar por ela. Atrás do `repeatable_schedules`, semeado desligado. Parte 4: o e-mail de entrada é reivindicado no Redis com `SET <chave> '1' EX 86400 NX` a partir do `Message-ID` (sha256 da mensagem crua quando não há), e a mesma chave vai junto como `jobId` do `api-v2`. A parte 3 foi para a E5 e a metade durável da parte 2 para a E4. Medido em duas réplicas reais dividindo um Postgres e um Redis, com cada controle rodado uma segunda vez contra uma cópia quebrada de propósito para ver a falha: trava de cron 1 executou / 1 pulou (quebrada: 2 intercaladas); schedules 1 entrada repeatable e 11 disparos em 21s alternando réplicas (quebrada: 2 entradas, 20 disparos); o disparo conclui em 14–68ms com uma execução por tick (quebrada: 3023ms); dois `Message-ID` idênticos produzem uma execução. Registrado no `S8-REPLICA-HARNESS.md` com as sondas em `scripts/s8-controls/`. Uma exceção honesta: o `listPop3Emails` registra handlers de evento do POP3 e retorna, então a transação dele commita enquanto a conversa ainda está em curso e 4 de 22 ticks rodaram nas duas réplicas ao mesmo tempo. A trava não é a culpada — ela é exclusiva sempre que o job espera o próprio trabalho — e quem protege o e-mail de entrada é a reivindicação no Redis, não a trava; cobrir o poll significaria manter uma transação do Postgres aberta pela conversa inteira a cada tick. O controle 5 também demonstrou o risco da E5 de forma concreta: sob `volatile-lru` 0 de 100 chaves de dedup sobreviveram a um enchimento; sob `noeviction` todas sobreviveram e quem levou o OOM foi a escrita que enchia. Validado em dev em 2026-09-21 com o flag ligado: cinco ticks consecutivos de dois minutos produziram exatamente uma execução cada, e apagar o agendamento parou as execuções no disparo seguinte sem reinício, com o consumer removendo o próprio repeatable. Duas coisas mudaram a forma de habilitar o flag: o reconcile vive no `onModuleInit`, então ligá-lo num processo em execução não registra nada até o próximo boot; e ele deixava o `delete()` no caminho novo enquanto o `CronJob` em memória criado no boot seguia rodando e cobrando, então o desligamento não fica mais atrás do flag. Passou no QA em 21/09/2026; aguardando na fila de deploy. |
| E3 | review | 868m0vk4t | Passed QA on 2026-09-21; waiting in the deploy queue. // Passou no QA em 21/09/2026; aguardando na fila de deploy. |
| A1 | planned | — | |
| D2 | planned | — | |
| A2 | planned | — | |
| A3 | planned | — | |
| A4 | planned | — | |
| A5 | planned | — | imageReaderNode left the task — deprecated (D24); an imageGenerator module is in progress on the dev line. // O imageReaderNode saiu da task — depreciado (D24); um módulo de imageGenerator está em andamento na linha de dev. |
| A6 | planned | — | D3 answered on 2026-09-02: documentSummarizer and commandMusicNode discontinued; webAmazon and secApiNode broken today (fix or drop, per type); fileSave under review. Six types get execution. // D3 respondida em 2026-09-02: documentSummarizer e commandMusicNode descontinuados; webAmazon e secApiNode quebrados hoje (corrigir ou descartar, por tipo); fileSave em revisão. Seis tipos ganham execução. |
| S6 | planned | — | The width of the first cut is the implementer's call (D25). // A largura do primeiro corte é decisão de quem implementar (D25). |
| A8 | planned | — | |
| A9 | planned | — | |
| D1 | planned | — | |
| B1 | planned | — | |
| B2 | planned | — | Starts by answering decision D1 — the decision is inside the task, not ahead of it. // Começa respondendo a decisão D1 — a decisão está dentro da task, não antes dela. |
| B3 | planned | — | Needs D2's measurement, taken by A1 in Wave 2; B3 answers D2 with it. // Precisa da medição da D2, feita pela A1 na onda 2; a B3 responde a D2 com ela. |
| B4 | planned | — | |
| E1 | planned | — | |
| E2 | planned | — | |
| B5 | planned | — | Preconditions: S1, S2, S3, E3. When the credit balance runs out with N generations already in flight, D20 lets all N finish — the overshoot is N nodes, not one, so size the headroom. D21 applies only if the queue actually chooses what runs next — confirm that first. // Pré-requisitos: S1, S2, S3, E3. Quando o saldo de crédito acaba com N gerações já em voo, a D20 deixa as N terminarem — o estouro é de N nós, não de um, então dimensione a folga. A D21 só vale se a fila realmente escolher o próximo item — confirme isso antes. |
| A7 | planned | — | |
| B6 | planned | — | Inherits D13 and D14 from S1 — do not re-open them. D23 makes container nodes cancellable rather than allowed to finish. Ships after B5 (D17). // Herda a D13 e a D14 do S1 — não reabra as duas. A D23 torna os nós contêiner canceláveis em vez de deixá-los terminar. Sobe depois da B5 (D17). |
| B7 | planned | — | The batch screen is out of this epic (2026-09-02). // A tela de lote está fora deste épico (2026-09-02). |
| C1 | planned | — | |
| C2 | planned | — | Executes the D2 answer; the measurement is A1's (Wave 2) and the answer is B3's (Wave 4). // Executa a resposta da D2; a medição é da A1 (onda 2) e a resposta é da B3 (onda 4). |
| E4 | blocked | — | Blocked on B4: a Temporal Schedule can only trigger a workflow, and no whole-flow workflow exists until B4 ships. S8 delivered the duplication and durability fix on Bull repeatable jobs (D26) rather than building a throwaway bridge workflow; E4 adds what that leaves out — pause, backfill, last-run state and durability independent of Redis. // Bloqueada pela B4: um Temporal Schedule só sabe disparar um workflow, e não existe workflow de fluxo inteiro até a B4 entrar. A S8 entregou a correção de duplicação e durabilidade em repeatable jobs do Bull (D26) em vez de construir um workflow-ponte descartável; a E4 acrescenta o que fica de fora — pause, backfill, estado da última execução e durabilidade independente do Redis. |
| E5 | planned | — | Split out of S8 Part 3 on 2026-09-18. Measured in the account: prod runs `volatile-lru`, which can evict the TTL-bearing dedup keys, but there have been zero evictions in 14 days at 1.9% memory and 1.8% engine CPU — debt with a bad blast radius, not an incident, so it did not hold S8. The same measurement answers the post-B5 sizing question: no resize is indicated. // Separada da parte 3 da S8 em 2026-09-18. Medido na conta: a produção roda `volatile-lru`, que pode despejar as chaves de dedup que têm TTL, mas houve zero evicções em 14 dias, com 1,9% de memória e 1,8% de CPU do engine — dívida com raio de dano ruim, não incidente, então não segurou a S8. A mesma medição responde ao dimensionamento pós-B5: não há indicação de resize. |
